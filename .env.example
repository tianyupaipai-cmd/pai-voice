# Never commit real credentials or private endpoints.
PAIVOICE_ASR_PROVIDER=mock
PAIVOICE_TTS_PROVIDER=mock
PAIVOICE_ADAPTER=mock

# Examples only: supply these locally when a provider requires them.
# OPENAI_API_KEY=
# GROQ_API_KEY=
# ELEVENLABS_API_KEY=
